<!DOCTYPE html>
<html>
<head>
<title>PhotoFind - Profile</title>
<meta name="viewport" content="width=device-width, initial-scale=1">
<link type="text/css" rel="stylesheet" href="/css/bootstrap.min.css">
<link type="text/css" rel="stylesheet" href="/css/readable.css">
<link type="text/css" rel="stylesheet" href="/css/profile.css">

</head>
<body>
<% include partials/navbar%>

<article class="jumbotron container">
   <h1>Profile</h1>
<section class="panel panel-default">
  <div class="panel-body">
    <article class="profile-details col-lg-4">
    <div id="profile-img" class="profile-img">
        <img src="https://robohash.org/<%= (newUser.local.username) ? newUser.local.username : user.local.username %>" class="img-responsive" id="profile-img"/>
      </div>
    </article>
    <article class="profile-details col-lg-4">
 <% if(user.local.username){%>
   <h3 id="username">Alias: <%= (newUser.local.username) ? newUser.local.username : user.local.username %></h3>
       <%}%>
      <% if(user.local.firstname){%>
   <h5 id="city">Full Name: <%= (newUser.local.firstName) ? newUser.local.firstName : user.local.firstName %>
       <%= (newUser.local.lastName) ? newUser.local.lastName : user.local.lastName %></h5>
          <%}%>     
    <% if(user.local.username){%>
   <h5 id="city">City: <%= (newUser.local.city) ? newUser.local.city : user.local.city %></h5>
          <%}%>
<% if(user.local.country){%>
   <h5 id="country">Country:  <%=(newUser.local.country) ? newUser.local.country : user.local.country %></h5>
       <%}%>
<% if(user.local.email){%>
   <h5 id="email">E-mail address: <%=(newUser.local.email) ? newUser.local.email : user.local.email%></h5>
       <%}%>
        <% if(newUser.local.email == user.local.email){%>
      <a href="/updateAccount" id="updateAcctLink"><button class="btn btn-success btn-xs">Update Account Info</button></a>
        <%}%>

    </article>
  </div>
</section>


<div class="row">

<div class="col-lg-4">
<article class="panel panel-primary">
  <div class="panel-heading">
    <h3 class="panel-title">Total Points</h3>
  </div>
  <div class="panel-body">
    <% if(user.local.totalPoints){%>
    <h2 id="totalPoints">  <%= (newUser.local.totalPoints) ? newUser.local.totalPoints : user.local.totalPoints %></h2>
    <%}%>
  </div>
</article>
</div>

<div class="col-lg-4">
    <article class="col-lg 4 panel panel-primary">
        <div class="panel-heading">
          <h3 class="panel-title">Badges</h3>
        </div>
        <div class="panel-body">
          <img class="badge-img" src="/img/badge3.jpg" alt="Noob badge - You were a noob once too!">
          <img class="badge-img" src="/img/badge1.jpg" alt="Points Leader - Broke the top 5!">
        </div>
    </article>
  </div>
  </div>
<br/>
<% if(newUser.local.email == user.local.email){%>
<section class="panel panel-primary">
  <div class="panel-heading">
    <h3 class="panel-title">Point History</h3>
  </div>
  <div class="panel-body">

<div>
    <table class="table table-striped table-bordered table-condensed" id="pointstbl">
                    <thead>
                        <tr>
                            <th>Date</th>
                            <th>Points Earned</th>
                            <th>Activity Type</th>
                        </tr>
                    </thead>

                </table>
                </div>
</div>
  </div>
</section>
<%}%>

</article>

<script src="/js/jquery.min.js"></script>
<script src="/js/bootstrap.min.js"></script>
<script src="/js/profile.js"></script>
</body>
</html>
